name: tutorials
version: 'master'
asciidoc:
  attributes:
    prerelease:
    is-beta: false
    release: '3.0'
    major: 3
    minor: 0
    maintenance: 0
    base: 0
    # Use to add a release number suffix eg for betas (3.0.0-beta02)
    releasetag:
    #
    # These overrides provide for testing by pointing to a different GH repo and or branch
    # Remove these overrides when not required
    #
    github_root_override: https://github.com/ibsoln/userprofile-couchbase-mobile/blob/
    github_branch_override: DOC-9382/standalone

